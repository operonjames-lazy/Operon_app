# Dependencies
node_modules/

# Local env
.env
.env.local
.env.*.local

# OS / editor
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea

# Logs
*.log

# Archives / exports
operon-tester-*.zip
operon-website-*.zip

# Loose design / verification screenshots at the repo root.
# Operon-dashboard's own public/ assets are scoped under its package.
/*.png
/*.jpeg
/*.jpg

# MCP / Playwright cache directories left by tooling.
.playwright-mcp/

# Stray root-level pnpm-lock — workspace lockfiles live inside each package
# (operon-dashboard/pnpm-lock.yaml). The root-level one was dropped from
# tracking in the monorepo restructure (commit a127009).
/pnpm-lock.yaml
