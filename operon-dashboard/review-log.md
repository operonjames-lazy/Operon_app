# review-log

Append-only log of ship-readiness reviews.

## 2026-04-18 | ship-readiness | 4 blocking (tester-round) + 4 blocking (mainnet-only), 14 required, 17 advisory | not ready
## 2026-04-18 | follow-up fixes | 8 blockers + 12 required items landed in commit(s); re-run `/review-ship` to validate | pending re-review
## 2026-04-18 | ship-readiness re-review 967fa04 | 0 blocking, 6 required (3 tester-visible i18n/401-race, 3 operational), 15 advisory | ready with caveats
## 2026-04-18 | follow-up fixes to re-review | 6 required + 15 advisory items closed; 3 non-blocking items deferred | pending third review
## 2026-04-19 | ship-readiness post-R5 fixes | 9 blocking, 15 required, 14 advisory | not ready
## 2026-04-21 | ship-readiness R6→R7 (contract role split + R6-BUG-01/02/03 fixes) | 3 blocking, 5 required, 8 advisory | not ready until blocking closed
## 2026-04-21 | follow-up fixes to R6→R7 review | 3 blocking + 5 required closed (guides 001–017 in EN+ZH, ZH private-RPC block ported, ChainSelector stuck-state fix, USDC_ADDRESS shell foot-gun clarified, discountBps ≤ 10000 cap + 2 new tests, test-count 51→64 in ARCHITECTURE/OPERATIONS, JWT_SECRET placeholder warning) | ready with caveats for R7 tester
## 2026-04-21 | regression-prevention + mainnet-path work | webhook audit + local test-webhooks.mjs harness + OPERATIONS §6.5 runbook; Playwright scaffolded (7 tests discovered, 1 UI smoke + 1 UI capture runnable, 4 full-chain stubbed with TODOs); Thai prose review of in-app strings (see next entry) | R7 tester-ready unchanged; mainnet still gated on (1) full-chain E2E wiring (~3–4h), (2) Gnosis Safe handover, (3) live Vercel webhook smoke
## 2026-04-21 | Thai prose native-reader review | EPP onboarding translations rated high (formal register consistent, legal terminology correct); main i18n translations had ~13 defects fixed: 5 P1 (ambiguous กระเป๋าจ่ายเงิน→กระเป๋ารับเงิน, wrong-sense สนับสนุน→ช่วยเหลือ, stiff ทรัพยากรที่ร้องขอ→ข้อมูลที่ต้องการ, ได้ลด→ได้รับส่วนลด ×2), 3 P2 consistency (ใช้งาน→กำลังขาย ×2, ทรัพยากรแบรนด์→สื่อแบรนด์), 5 P3 polish (classifier + existential + phrasing). 2 minor EPP tweaks (preposition in p3, legalese in sec[5]). Optional transliteration nits + spelling normalisation left untouched | next build green
## 2026-04-22 | ship-readiness cycle-2 whole-repo | 7 blocking, 26 required, 19 advisory | not ready until blocking closed
## 2026-04-22 | Phase 1 livenet-prep fixes | B1 admin-remove terminal 'revoked' status (migration 018 + remove/route.ts + drain queries + validate-code reason); B6 QuickNode topic0 in OPERATIONS.md corrected to 0x6591bdbb… with recompute command; B2 migrations 017+018 added to OPERATIONS §2 history + cron-auth mechanism reworded + hardhat count 51→64; B4 useTierRealtime tracks prior subscribe status + invalidates sale/dashboard queries on reconnect edge; B5 pending-tx guard requires strict address match + skips write when address undefined; JWT_SECRET placeholder guard added to lib/auth.ts (mainnet+prod = refuse boot on known placeholders). Build green; typecheck clean. B3 comment drift + B7 E2E scaffold deferred. | livenet-ready pending Phase 3 ops work (Gnosis Safe, live smoke test, mainnet contract deploy, webhook reconfigure)
## 2026-04-22 | admin panel review | security+data+api+client+ops+compliance + REVIEW_ADDENDUM | 0 blocking, 4 required, 3 advisory | approve with comments
- D-9 → REAL ISSUE: unbounded SELECTs on `purchases`, `referral_purchases`, `epp_partners`, `referrals` across 6 read endpoints + lib/admin-read.ts. At Genesis-sale scale (100k purchases) PostgREST implicit row caps silently truncate aggregates, producing wrong revenue/attribution/commission totals on Overview and Payouts. Fix: move aggregation to Postgres RPC (`admin_overview_stats()`, `admin_attribution()`, `admin_daily_revenue()`, `admin_unpaid_grouped()`), mirroring addendum D-P3's "prefer RPC for money math" pattern.
- Pass 3 → REAL ISSUE: `/api/admin/users/[id]` caps `referral_purchases` at 500 rows then sums them as "lifetime commissions" — Senior+ EPP partners whose commission-row count exceeds 500 see under-reported totals on their detail page. Fix: split the aggregate query from the recent-list query.
- O-P5 → REAL ISSUE: 4 new mainnet token-address env vars read by `/api/admin/sale/balance` are not in OPERATIONS.md §1 or the Vercel env table. Missing env = balance tiles show "n/a" with no hint; also a mainnet-deploy foot-gun.
- C-P4 → DELIBERATE DEVIATION: all admin JSX is hardcoded English. Documented here as intentional (operator-only, one user) so it's not read as drift by the next reviewer; either formalise via comment in the admin layout or route strings through `t()` with an `admin.*` namespace.
## 2026-04-26 | ship-readiness | 5 blocking, 11 required, 12 advisory | not ready
## 2026-04-26 | livenet drift remediation | applied 4 migrations missing from live Supabase (014/019/021/023) — discovered via runtime DB probe that prior 8 review rounds missed; killswitches inert, suspended-partner filter absent, tier 6-40 unreachable, all silently fail-open. Audit captured 0 exposure on bad commissions (no partners suspended yet). 28 ship-readiness findings landed in code/docs; tsc/hardhat/next-build green. Operator-owed for mainnet: Vercel env audit, vendor webhook resub, wagmi v3 smoke test, Gnosis Safe novation. | livenet-ready pending operator-owed list
- Pass 5 orphan inverse → REAL ISSUE: `/api/admin/partners/status` is built with audit+reason but not wired into any UI — operator will forget it exists. Wire a suspend button on the user-detail page or remove the endpoint.
- S-76/A-8 → ADVISORY: user search strips ILIKE wildcards but not PostgREST `.or()`-filter special chars (`,`, `.`, `(`, `)`, `:`). Garbage results for queries containing these, not a security issue (admin already has read access).
- Pass 3 → ADVISORY: user-detail page headers say "Purchases · {N}" / "Referrals made · {N}" where N is the truncated query length, not the true count. Fix with `count: 'exact', head: true` shadow queries.
- NO CHECK CAUGHT: D-9 exists but only at Quick Review level; for multi-aggregate admin dashboards where each tile does its own scan, the cumulative silent-truncation pattern deserves its own project-specific check. Candidate for a new addendum entry ("D-P9. Admin dashboards aggregate via RPC, not client-side sum over unbounded SELECT") if this pattern recurs.
## 2026-04-22 | admin panel review-required closures | O-P5 + Pass-5 + D-9 + Pass-3 (+ Pass-3 advisory count-headers) | 2 advisory remain: C-P4 (admin JSX EN-only, deliberate), S-76/A-8 (user search .or() special-char escape) | admin panel review-clean for blocking+required items
- O-P5 closed in `d46cea4` — OPERATIONS §1 + §3 env docs
- Pass-5 closed in `a26ae08` — `/admin/users/[id]` now has Change-status form → `/api/admin/partners/status`
- D-9 + Pass-3 closed by migration 020 (`admin_attribution`, `admin_overview_stats`, `admin_daily_revenue`, `admin_unpaid_grouped`, `admin_user_commission_totals`) + `lib/admin-read.ts` rewrite + `/api/admin/payouts/unpaid` rewrite + `/api/admin/users/[id]` aggregate-split with exact-count shadow queries. REVIEW_ADDENDUM gains new **D-P9** ("Admin dashboards aggregate via Postgres RPC, not client-side sum over unbounded SELECT"). Applied to hosted Supabase; verified all 5 RPCs against live dataset (lifetime $1,338.75 across 2 purchases matches, unpaid $160.65 matches).
## 2026-04-22 | admin panel advisory closures | C-P4 + S-76/A-8 | 0 open | admin panel fully review-clean
- C-P4 closed — `app/(admin)/admin/layout.tsx` gains a header comment documenting English-only admin JSX as deliberate (operator-only, single user). Revisit trigger: a second admin who doesn't read EN.
- S-76/A-8 closed — `/api/admin/users/search` strip regex extended from `[%_]` to `[%_,()"\\]` to remove PostgREST .or()-filter metachars that previously produced silent garbage results. Deliberately keeps `.`, `:`, `@`, `-` since they're safe inside .or() values (only structural in the key half of `field.op.value`), so email / UUID / OPR-XXXX / wallet searches still function.

## 2026-04-27 | data + reliability + ops + compliance | Phase 1 (sale_reservations) + Phase 2 (NodeSale v2 voucher contract) | 2 required, 3 advisory, 1 praise
- D-P1 (money is integer cents) → REAL ISSUE: none — `unit_price_cents BIGINT`, `discount_bps INTEGER`. Clean.
- D-P4 (idempotency via UNIQUE) → REAL ISSUE: none — `complete_reservation` rides on `tier_increments` PK; `usedReservations[bytes32]` enforces voucher-side replay.
- D-P6 (applied migrations immutable) → REAL ISSUE: none — 026 is new; no edits to applied files.
- O-P1 (migrations applied via script, not edited) → REAL ISSUE: none.
- O-P2 (build + tests pass) → REAL ISSUE: none — tsc 0, hardhat 53/53, lint 0 errors / 43 warnings.
- R-P1 (reconcile branches on kind) → REAL ISSUE: none — new failed_events inserts in `route.ts:174-225` use `kind='process_error'` and `kind='tier_increment_error'` matching existing retry-pass branching.
- R-P5 (cron lookback covers gap) → REAL ISSUE: none — preserved at `route.ts:127-128`.
- **Co-P3 (terms_version increments when T&Cs change) → REAL ISSUE**: commit `e2db200` edited section 4 of EPP T&C in 6 languages but did NOT bump `TERMS_VERSION = '1.0'` in `app/epp/onboard/page.tsx:43`. Required fix.
- **O-P5 (env var docs current) → REAL ISSUE**: Phase 2 deploy.ts adds `VOUCHER_SIGNER_ADDRESS`, `LOCAL_TIER_CAP`, `ADMIN_CAP_PER_TIER` env reads not in `docs/OPERATIONS.md` or `.env.example`. Required fix.
- NO CHECK CAUGHT (advisory): Pass 1 — `complete_reservation` accepts `'expired'` reservations for recovery and increments `total_sold` without checking against `total_supply`. If a freed-and-recovered reservation collides with a new reservation that took the freed slot, `total_sold` can exceed `total_supply` for that tier. Bound is the contract's `localTierCap` per-chain; backend total_sold drift is operationally reconcilable but not currently alerted.
- NO CHECK CAUGHT (advisory): `setLocalTierCap` comment claims "Lowering below localTierSold simply prevents further purchases until localTierSold catches up" — but `localTierSold` is monotonically increasing, so it never catches up downward. Permanent block until cap raised.
- NO CHECK CAUGHT (advisory): `adminMint` records `tierMinPrice[tierId]` as the per-node price on `OperonNode`. For a tier where setTierMinPrice was never called, this is 0. No revert; produces nodes with price=0 audit record.
- PRAISE: Phase 2 contract diff is a meaningful simplification (-1107/+788). Strict CEI in `purchaseWithVoucher`, comprehensive test coverage of voucher binding (buyer / chain / contract / deadline / replay / signer / tampered payload). Migration 026's `reserve_node_purchase` uses `FOR UPDATE` on the active tier as the load-bearing concurrency primitive and returns structured-error envelopes for client display.


## 2026-04-27 | Phase 5 cleanup + F66.1 + migration drift fix | security+data+api+reliability+ops + REVIEW_ADDENDUM | 0 blocking, 2 required, 2 advisory, 2 praise | approve with comments
- Pass 5 orphan inverse → REAL ISSUE: `mark_reservation_failed` (migration 026) has zero callers. All four `verifyOnChain('failed')` branches (alchemy/quicknode webhooks, cron reconcile retry, dev indexer-ingest) abandon the failed_events row but don't link the reservation. A reverted-tx purchase leaves its `sale_reservations` row in 'submitted' forever; `expire_old_reservations` deliberately ignores 'submitted' so inventory leaks against the active tier cap. Pre-existing voucher-design gap (Phase 3, D31), surfaced now by F66.1's full webhook-flow review.
- Pass 4 → REAL ISSUE: webhook ABI is v2-only — no fallback to v1 `NodePurchased` topic0. Until operator deploys v2 contract AND updates `SALE_CONTRACT_*` env AND reconfigures Alchemy/QuickNode subscription topic0, no purchase events parse. Failure mode is "silent skip → buyer pays, dashboard never reflects". Operator-checklist item, not a code fix.
- D-P9 → REAL ISSUE (pre-existing): `/api/admin/health` reads `failed_events` via `db.from(...).select('status, created_at')` and JS-reduces. Unbounded SELECT under PostgREST's implicit row cap. Pre-existing pattern, surfaced by the syncQueue removal review. Move to RPC.
- Pass 1 → ADVISORY: legacy v1 `failed_events.kind='tier_increment_error'` rows would explode in the new retry path (`bytes32ToUuid(undefined)` → throws → retry++ → 5x → abandoned + Telegram). Live-DB probe shows `failed_events: []` so academic, but worth a kind-guard if any accumulate before contract cutover.
- PRAISE: new `scripts/verify-pending-migrations.mjs` caught migrations 025+026 unapplied — Phase 3+4 voucher code was live in repo but the DB layer wasn't. Closes R15's "no automation enforces apply" gap. Wire into `/review-ship` step 0.
- PRAISE: `verifyOnChain` field-mismatch list now includes `reservationId` (process-event.ts:228) — closes a forge surface that the v2 design otherwise widens (since reservationId is the inventory-tracking pivot).
- NO CHECK CAUGHT: Pass 5 orphan inverse caught the unused `mark_reservation_failed` — there's no addendum check that says "every public RPC introduced by a migration must have a caller within N sessions of the migration landing." Candidate for a new R-Pn check after one more occurrence.

## 2026-04-27 | REVIEW_ADDENDUM gap-fill from external review | 5 new project-specific checks added
Five new checks filed for the categories my internal /review-full pass missed earlier today:
- D-P10 — Postgres function EXECUTE permissions revoked from PUBLIC + anon + authenticated. Filed against the missed Critical (RPC public-execute via anon key in browser).
- A-P7 — Inter-layer numeric bounds match the most restrictive consumer (DB CHECK / RPC validate / API route / contract require). Filed against the missed Medium (SQL allowed quantity≤1000 vs contract MAX_BATCH_SIZE=100).
- R-P6 — Webhook event → DB-state convergence is one atomic RPC with full field verification. Filed against the missed High (complete_reservation didn't verify event fields against the reservation row; commission write before reservation linking).
- O-P7 — Threat-model claims in DECISIONS.md are grep-verified against code constants. Filed against the missed Medium (D31 cited MAX_DISCOUNT_BPS but the contract has no such constant).
- O-P8 — Refactor sweep: every caller of the OLD path is updated when a new path becomes canonical. Filed against the missed High (admin/dev replay routes still on legacy increment_tier_sold after F66 made complete_reservation canonical).

## 2026-04-27 | ship-readiness correctness review (post mig 030) | 1 blocking, 2 required, 3 advisory | not ready

- D-58 / D-P1 (BLOCKING): mig 030 `process_purchase_with_reservation` `v_expected_amount` math diverges from `NodeSale.sol` `purchaseWithVoucher` for tiers 3/4/5 with EPP discount (1500 bps). Contract emits floor((unitPrice_token * qty * (10000 - discountBps)) / 10000) = 51658 cents for tier 5; mig 030 expects gross_cents - floor(gross_cents * d / 10000) = 51659. Contract: `totalPaid = unitPrice*qty - (unitPrice*qty*discountBps/10000)` in token base units, then `tokenAmountToCents` floors. Off by 1 cent whenever `(unit_price_cents*qty*discount_bps) % 10000 ≠ 0`. Confirmed numerically for tiers 3 (46856 vs 46857), 4 (49198 vs 49199), 5 (51658 vs 51659) on both Arb (6dec) and BSC (18dec) at qty=1. Net effect: every EPP purchase at those tiers lands `amount_mismatch`, queues to `failed_events`, and is `abandoned` after 5 retries — buyer paid + minted on-chain but DB has zero record. The mig 030 comment ("mirrors NodeSale.sol discount math exactly") is the opposite of what the SQL does; the OLD form in mig 028 (`(gross*(10000-d))/10000`) actually matched. Fix: revert mig 030's RPC body to the mig-028 form, or compute in token base units inside SQL using `discount_bps`+`token_decimals` columns to mirror the contract bit-for-bit.
- R-P2 (REQUIRED): when `process_purchase_with_reservation` returns a structural mismatch (`amount_mismatch` etc.), `lib/commission.ts:303` throws and `lib/webhooks/process-event.ts:386` queues with kind=`reservation_link_error` and 5-min initial backoff (NOT the immediate Telegram fast-path). The Telegram fast-path in `lib/commission.ts:31` only fires inside `processPurchaseWithReservation` when the RPC returns an envelope `{error}` — which it does — so the alert fires, but the addendum's PROGRESS.md item-5 noted these should page immediately. With mig 030's drift bug above triggering on every EPP T3-5 purchase, the on-call would receive a Telegram per affected purchase and 5 retries of each before abandon. Verify alerting volume is acceptable or the bug is fixed before live.
- D-P10 (REQUIRED): `instrumentation.ts` does NOT schema-assert anything on cold start — it only loads Sentry. The journey ledger in this review's prompt and several docs claim "instrumentation.ts schema-asserts on cold start", but the file is 14 lines that just import Sentry. mig 030's EXECUTE-revoke and column-grant changes have no runtime guard; `scripts/verify-pending-migrations.mjs` is the only check, and it's run manually. Either add the schema assertions the docs reference, or strike the doc claim. Consequence today: an operator could deploy the app against a DB where mig 030 wasn't applied and only learn from a forged anon RPC call — there's no boot-time tripwire.
- R-87 (ADVISORY): no remaining application caller of legacy `increment_tier_sold(...)`, `complete_reservation(...)`, `process_purchase_and_commissions(...)` outside the new wrapper. NodeSale v2's `purchase()` removal is clean — only `purchaseWithVoucher` is reachable from frontend (`app/(app)/sale/page.tsx`) and there are no leftover ABI references to v1 `purchase`. Phase 5 deletions (`sync-on-chain.ts`, `/api/admin/referrals/{remove,reset}`, `/api/admin/sale/tier-active`, `/api/dev/drain-referrals`) verified absent.
- D-P4 (ADVISORY): `purchases.tx_hash` is `UNIQUE` per migration 001 (not `UNIQUE(tx_hash, chain)`). Fine because tx_hashes are 32-byte values and Arb/BSC collisions are negligible, but the addendum journey-ledger asserts `(tx_hash, chain)`. Doc-vs-schema drift; not exploitable.
- O-P7 (ADVISORY): voucher signer compromise bound. Per `DECISIONS.md` D31 the bound is `tierMinPrice × MAX_DISCOUNT_BPS × MAX_BATCH_SIZE`. Confirmed `MAX_DISCOUNT_BPS = 1500` and `MAX_BATCH_SIZE = 100` exist in `NodeSale.sol:83-84` (the addendum O-P7 originating finding has been remediated since the doc is now grep-verifiable). Praise.

Recommendation: request changes — fix the mig 030 math drift before any livenet smoke test. Stop-the-line bug.

## 2026-04-27 | ship-readiness scale review | 1 blocking, 3 required, 4 advisory | not ready

Independent re-verification of the post mig-030 ship state. Pass 6 + ship-readiness extensions O-114/O-115/O-116. Build verification first: `npx tsc --noEmit` clean from repo root, `cd contracts && npx hardhat test` green (58 passing in 676ms — note: `LIVENET_TEST_RUNBOOK.md §0` says "all 54 must pass" and `OPERATIONS.md §6` says "64 tests must pass"; actual count is 58).

- D-58 / D-P1 (BLOCKING — RECONFIRMED): `supabase/migrations/030_lock_public_schema_and_rounding.sql:139-141` `v_expected_amount := v_gross - ((v_gross * v_res.discount_bps) / 10000)` produces +1 cent vs the on-chain amount whenever `(unit_price_cents * qty * discount_bps) mod 10000 ≠ 0`. Worked numerically across both chains: tier 3 (price 55125 cents, bps 1500) → contract emits 468562500 USDC base units → `tokenAmountToCents(…, 6) = 46856`; mig 030 expects 46857 → `amount_mismatch`, event quarantined to `failed_events`, abandoned after 5 retries. Same bug at qty=1 for tier 4 (49198 vs 49199) and tier 5 (51658 vs 51659). The mig-028 form `(unit_price_cents * qty * (10000 - discount_bps)) / 10000` was correct because both `floor` operations apply at the same scale (cents); the contract's `gross_token - floor(gross_token * d / 10000)` is mathematically the same as `floor(gross_token * (10000-d) / 10000)` when `gross_token * d` is a multiple of 10000 (which it always is at 4-or-more-decimals-of-headroom in token base units). After the contract amount divides by `10^(decimals-2)` in `tokenAmountToCents`, the fraction is dropped — landing on the mig-028 form's value, not mig-030's. Mig-030's "mirrors NodeSale.sol discount math exactly" comment is the opposite of what the code does. Same finding as 2026-04-27 entry above; still open at HEAD. Fix: revert the `v_gross := …; v_expected_amount := v_gross - …` block in mig 030 to mig-028's `(unit_price_cents * quantity * (10000 - discount_bps)) / 10000`, or land a new migration (031) with the correction. Hardhat test "handles odd-price rounding via integer division" passes because it operates in pure token units (no cents round-trip); add a Postgres-side regression test calling `process_purchase_with_reservation` with tier-3 inputs to catch this in CI.

- O-114 / O-P5 (REQUIRED): two env vars read by code that are absent from `.env.example`. (1) `NEXT_PUBLIC_E2E` read at `lib/wagmi/config.ts:83` (gates the E2E mock-wallet connector) — set automatically by `playwright.config.ts` but a fresh tester reading the runbook would not know it exists. (2) `DEV_INDEXER_URL` read at `scripts/dev-indexer.mjs:58` with default `http://localhost:3001`. Both are dev/test-only so impact is low, but they trip the addendum's O-P5 ("Env var documentation in OPERATIONS.md stays current") and the global O-114 census. Add both to `.env.example` under a "Test/E2E (optional)" section.

- O-116 (REQUIRED): the `OPERATIONS.md §1` Prerequisites table (lines 14-19) says **Node.js ≥22 LTS** and `pnpm ≥9`, but `package.json` does not pin engines. A fresh tester on Node 18 would `pnpm install` successfully and only discover the requirement when Next 16 / React 19 trip a runtime error. Add `"engines": { "node": ">=22", "pnpm": ">=9" }` to `package.json` so the failure surfaces at install time, not at first dev-server boot. (Static walk only; not blocking.)

- O-116 (REQUIRED): `OPERATIONS.md §6` line 471 says "64 tests must pass" and `LIVENET_TEST_RUNBOOK.md §0` line 21 says "54 tests"; the actual `cd contracts && npx hardhat test` shows **58 passing**. A reader checking the gate at the wrong number panics or skips the discrepancy. Sync both docs to 58 (or update both to "all hardhat tests pass" and stop counting). Hardhat test count appeared to drift here without a doc update for at least three sessions — consider replacing the literal in both docs with a verb-only assertion.

- D-P10 (ADVISORY): `instrumentation.ts` claim drift. The journey ledger and `LIVENET_TEST_RUNBOOK.md §"Process change owed"` line 4 reference an instrumentation.ts schema-assert on cold start; the file is 14 lines that only initialise Sentry. Per the runbook the schema-check is correctly labelled as a post-launch item, but the journey ledger and at least one PROGRESS entry imply it ships now. Strike the claim from the prompt/doc, or land the schema-asserts. Same finding as the prior 2026-04-27 entry; carrying forward.

- O-115 (ADVISORY): trust-critical major-version bumps still post-knowledge-cutoff and unverified end-to-end on the wagmi v3 connector lifecycle: `wagmi@3.6.4`, `@rainbow-me/rainbowkit@2.2.10`, `next@16.2.2`, `react@19.2.4`, `viem@2.47.19`, `@openzeppelin/contracts@5.x`. Per `LIVENET_TEST_RUNBOOK.md §5` "this is the single highest-priority unverified piece" — gate not closed; smoke before mainnet promotion. No breaking-API matches in the code (jose v6 `jwtVerify` API stable; OZ v5 `Ownable(msg.sender)` ctor pattern correctly used in `NodeSale.sol:144`).

- O-P5 (ADVISORY): `.env.example:36` declares `VOUCHER_SIGNER_ADDRESS` next to deployment vars but the *consumer* docs are spread across `OPERATIONS.md §1` (lines 71-81) and `LIVENET_TEST_RUNBOOK.md §1` (lines 66-69) without cross-reference. A rotation operator following only the runbook will miss the OPERATIONS.md note that the same key is read by the API server (not just deploy.ts). Add a one-liner cross-link in either direction; the runbook is the action-doc and currently the more authoritative place.

- Pass-6 / Scale (ADVISORY): `reserve_node_purchase` (mig 028 line 73) takes `FOR UPDATE` on the active `sale_tiers` row before computing `v_available`. Every concurrent reservation across both chains serialises through this single row. At a tier boundary on launch day with 1000 concurrent buyers, throughput is bounded by `2 * round_trip_to_pg + tx_commit` — likely 50-200 reservations/sec in practice. Acceptable for a 1250-supply tier (drains in 6-25 seconds), but if Vercel function concurrency or Supabase pool exhaustion happen first, requests will 504 with no clear signal to the buyer. Tracking only — load-test the reserve path against a hosted Supabase instance before mainnet, with the cron and webhook traffic running concurrently.

Recommendation: request changes — the mig 030 amount-math bug is still open and is the same blocker the previous session flagged. Until it lands, no testnet smoke run will produce a single successful EPP-discount purchase at tiers 3-5+ end-to-end through the DB. The other findings are non-blocking but should land in the same sweep so the env-var census + Node-version + hardhat-count drift are closed in one pass.

## 2026-04-27 | ship-readiness client review (Pass 4) | 0 blocking, 1 required, 3 advisory | approve with comments

**Scope:** Pass 4 (state and lifecycle) plus ship-readiness extension. Whole-system fresh checkout. Build verified clean (npx tsc --noEmit returns 0).

**Build verification:** typecheck passes at HEAD (commit 7cb0b31). No type errors.

**Methodology:** Walked the journey ledger steps 1 to 13 end-to-end through React Query keys, useAuth state machine, useTierRealtime reconnect path, sale page voucher countdown, and /nodes pending-attribution banner. Audited mig 30 grants vs every browser-side getSupabaseBrowser() call site. Verified i18n key parity across all 6 languages for the new mig-30/voucher copy (selfReferralBlocked, voucherExpired, reservedExpiresIn, reserveAtPrice, realtimeOffline -- all present 6x).

### Findings

#### required -- app/(app)/sale/page.tsx:539, 636, 669 (sale-page direct fetch calls)
**Pass/Check:** Pass 4 (state and lifecycle) -- auth-cookie expiry recovery (C-10).
**Entry path:**
  1. app/(app)/sale/page.tsx:669 -- handleReserve calls raw fetch on /api/sale/reserve (not authFetch).
  2. app/(app)/sale/page.tsx:539 -- fire-and-forget fetch on /api/sale/reservations/submit (not authFetch).
  3. app/(app)/sale/page.tsx:636 -- validateCode calls raw fetch on /api/sale/validate-code (not authFetch).
  4. lib/api/fetch.ts:36 -- only authFetch dispatches the operon:auth-expired event; the wrapper that fires the event is bypassed.
  5. hooks/useAuth.ts:261-280 -- listener never fires, so isAuthed stays true.
**Finding:** If JWT_SECRET rotates or the operator force-logs-out the user mid-session, the operon_session JWT becomes invalid server-side while the non-httpOnly operon_auth=1 flag cookie keeps isAuthenticated() returning true. Sale page three direct fetch calls 401, but the auth-expired event never dispatches; useAuth keeps isAuthed=true; the merged effect at L145 will not re-run SIWE. The buyer sees Sign in first and has no recovery path short of explicit Disconnect-then-Reconnect. The cookie ALSO has maxAge 24*60*60 seconds (matches JWT) so natural expiry self-heals, but secret-rotation and admin force-logout do not. Inferred-from-code-only -- not exercised in tester runs because tester JWTs do not outlive the test window.
**Fix:** Replace the three raw fetch calls in app/(app)/sale/page.tsx (lines 539, 636, 669) with authFetch from lib/api/fetch.ts. The 401 listener already handles the rest of the recovery sequence correctly (clearSession, then invalidate, then re-authenticate).

#### advisory -- app/(app)/sale/page.tsx:1165-1172 (Purchase button expiry guard)
**Pass/Check:** Pass 4 -- feature time-window state.
**Finding:** The Purchase button disabled clause does not include nowMs >= reservation.expiresAt. Auto-expiry runs from a 1Hz setInterval (L508) gated on [reservation]. When the tab is backgrounded, browsers throttle interval to 1/min, so nowMs lags Date.now() by up to 60s. A user returning to the tab can see Reserved expires in 02:43 while real time is past expiresAt; clicking Purchase dispatches the on-chain tx which the contract reverts with voucher.deadline less-than block.timestamp, costing gas. The contract is correct; the UI is misleading.
**Fix:** Add Date.now() >= reservation.expiresAt (or just nowMs >= reservation.expiresAt, accepting the 1s race window for active tabs) to the Purchase button disabled clause. Optionally also bind the auto-expiry effect to document.visibilitychange so resume-from-background triggers an immediate setNowMs(Date.now()) re-check.


## 2026-04-27 | ship-readiness security review (post mig 030, fresh checkout) | 1 blocking, 4 required, 4 advisory | not ready

Scope: whole system, dashboard. Probe-verified migration 030 against live DB via service-role + anon REST. Numerically verified contract↔mig 030 discount math drift. Did not run hardhat tests in this pass.

- D-P1 / D-58 (BLOCKING, regression of prior fix): mig 030's `process_purchase_with_reservation` body (line 140) computes `v_expected_amount := v_gross - ((v_gross * v_res.discount_bps) / 10000)` in CENTS-space, but `NodeSale.sol:206-210` computes `totalPrice = unitPrice*qty - (unitPrice*qty*discountBps/10000)` in TOKEN-base-units, and the webhook converts it back to cents via floor in `tokenAmountToCents`. Empirically verified (script in /tmp/probe-tg.mjs): every tier ≥ 3 with either community (1000 bps) or EPP (1500 bps) discount produces `mig030 = contract + 1`. Example: tier 5 EPP → mig 030 expects 51,659 cents, contract emits 51,658. Mig 028's earlier form `floor(gross*(10000-d)/10000)` matched the contract exactly; mig 030's comment ("mirrors NodeSale.sol exactly") is wrong. Net effect: every discounted T3+ purchase trips `amount_mismatch`, queues `reservation_link_error` to `failed_events`, abandons after 5 retries; buyer's USDC + minted node persist on-chain with no DB commission/inventory write. **Same blocking finding the 2026-04-27 internal review filed; not yet fixed.** Fix: revert mig 030's RPC body to mig 028's `(unit_price_cents * quantity * (10000 - discount_bps)) / 10000`.

- S-P4 / RLS surprise on sale_config (REQUIRED, probe-verified): mig 030 grants `SELECT (id, stage, public_sale_date, tier_max, community_discount_bps, epp_discount_bps, realtime_enabled, updated_at) ON sale_config TO anon, authenticated`. Probed live DB: anon SELECT of granted columns returns `[]` (status 200, content-range `*/0`); service-role returns the seeded row 1. RLS is enabled on `sale_config` somehow — no migration explicitly enables it, but mig 004 lists `sale_config` as one of the tables it does NOT disable RLS for. Consequence: `useTierRealtime.ts:87` subscribes to `postgres_changes` on `sale_config` but Realtime delivers events under the user role and applies RLS — so an admin flipping `stage` from `whitelist`→`active` will not propagate to live browsers; the sale page only refreshes via the 10s TanStack poll. Fix: add `ALTER TABLE sale_config DISABLE ROW LEVEL SECURITY;` (matching mig 004's pattern for sale_tiers) in a new migration, or add a permissive SELECT policy scoped to the granted columns. Without this, mig 030's narrow grant is a no-op for `sale_config`. (sale_tiers verified anon-readable: 40 rows visible, RLS off.)

- D-P10 / instrumentation gap (REQUIRED, code-only): `instrumentation.ts` is 14 lines and only loads Sentry — it does NOT schema-assert anything on cold start, despite `LIVENET_TEST_RUNBOOK.md:245` documenting "Startup schema check — refuse to boot if expected tables/functions are absent in production" as an owed item. The journey ledger in this review's prompt also asserts "instrumentation.ts schema-asserts on cold start". An operator who deploys the dashboard against a Supabase DB where mig 030 (or any later migration) wasn't applied has no boot-time tripwire — first symptom is permission errors at runtime. Fix: implement the schema-assert per the runbook, or strike the claim from the runbook + prompt + DECISIONS.md.

- R-P1 / cron untriggered locally + reservation expiry gap (REQUIRED, journey mode): `pnpm dev` does not run Vercel cron. `scripts/dev-indexer.mjs` invokes `/api/dev/replay-failed-events` every poll (covers failed-event retry) but never calls `/api/cron/reconcile` and never calls `expire_old_reservations` directly. Consequence in the local test journey: reservations a tester creates and abandons accumulate as `'reserved'` forever, count toward `v_active_reserved` in `reserve_node_purchase` (mig 028:85-92), and after enough abandons a wallet's tier shows as full to that wallet's next reservation. `TESTING_GUIDE.md` does not tell the tester to manually `curl /api/cron/reconcile` or invoke any expiry path. Two acceptable fixes: (a) add an `expire_old_reservations` call to `dev-indexer.mjs`'s loop, mirroring the `replayFailedEvents()` pattern; or (b) add a §3.8.2 step to TESTING_GUIDE telling the tester how to call reconcile with `CRON_SECRET`. (a) is lower-friction.

- D-P10 probe coverage (REQUIRED, process gap): `scripts/probe-anon-exposure.mjs` (1) tests `selectTable(...)` with no `?select=` parameter, which always returns 401 even when columns ARE granted because `created_at`/`updated_at` are not in mig 030's GRANT list — so a future regression that re-grants those would not be caught (or, conversely, a passing run does not prove mig 030's narrow grants work); (2) does not include `sale_config` in the table list at all, despite mig 030 making it the second narrow-grant table. `scripts/probe-rpc-privileges.mjs` is general (scans every public function) and remains correct. Fix: in probe-anon-exposure, replace `selectTable(t)` with `selectTable(t, '?select=<granted-cols>')` for the two tables mig 030 grants, and add `sale_config` to both the granted-list test and a separate "anon should NOT see ungranted columns" assertion. Without this, the probe's "all 401" output looks like a clean lock-down but actually masks an RLS misconfiguration on sale_config (see the S-P4 finding above — caught manually this round, would have been caught by a richer probe).

- A-P7 (ADVISORY, defense-in-depth): mig 030's `process_purchase_with_reservation` re-validates `quantity > 0 AND quantity <= 100` (line 84) but mig 028's `sale_reservations.quantity_check` constraint is `quantity > 0 AND quantity <= 100` and contract `MAX_BATCH_SIZE = 100` — all aligned. Praise. Same with `discount_bps <= 1500` (RPC line 58, contract `MAX_DISCOUNT_BPS = 1500`). No wedges seen.

- O-P8 sweep (ADVISORY, verified clean): no remaining application caller of legacy `increment_tier_sold(...)`, `complete_reservation(...)` outside the new `process_purchase_with_reservation` wrapper. `app/api/admin/events/replay/route.ts` and `app/api/dev/replay-failed-events/route.ts` both go through `processPurchaseWithReservation` correctly. Phase 5 deletions of `referral_code_chain_state` references all clean.

- S-P3 (ADVISORY, code-only): `lib/admin-signer.ts:57` uses a bare `catch {}` after `new ethers.Wallet(key, provider)` — this is fail-closed (returns `signer_init_failed`) and avoids logging the key. Praise. `lib/voucher.ts:147-153` has the same hygiene for VOUCHER_SIGNER_PRIVATE_KEY. No leak surfaces found.

- S-P5 verified (PRAISE): `lib/nonce.ts:63` uses Redis `DEL` to atomically consume the SIWE nonce — no TOCTOU. Memory-store fallback gated on `NODE_ENV !== 'production'` via `requireRedisInProd()`. Solid.

- S-P7 verified (PRAISE): `lib/auth.ts:8-37` mainnet-strict default — anything except explicit `NEXT_PUBLIC_NETWORK_MODE=testnet` triggers the placeholder guard. Both the `.env.example` value and the older `'change-this-...'` string are blocklisted.

Recommendation: not ready. Block on the discount-math regression (D-P1/D-58); that bug alone takes every discounted T3+ purchase off the books. The S-P4 RLS-on-sale_config issue is fixable in a one-line migration but should land before public sale opens or admin stage flips will not propagate to browsers. The cron-not-local gap is a journey caveat for testers; the instrumentation schema-assert gap is paper-debt that should be settled before mainnet either way.
#### advisory -- hooks/useAuth.ts:236-245 and app/(app)/sale/page.tsx:562-580 (operon_pending_attribution not cleared on disconnect/wallet-switch)
**Pass/Check:** Pass 4 -- state across logout/login boundary (C-3).
**Finding:** Both the useAuth disconnect handler (L240) and the sale-page wallet-switch handler (L577) explicitly remove operon_pending_tx but leave operon_pending_attribution in localStorage. The /nodes reader (readPendingAttribution) filters by wallet match so wallet B never shows wallet A marker, but the marker survives indefinitely on shared devices until the 15-min TTL expires or wallet A reconnects. Not user-visible but inconsistent with the operon_pending_tx hygiene at the same call sites and creates a small information-disclosure surface -- a forensic look at localStorage shows what wallet A was buying.
**Fix:** Add localStorage.removeItem with key operon_pending_attribution to both handlers alongside the existing operon_pending_tx cleanup. One line each.

#### advisory -- hooks/useTierRealtime.ts:34-125 (no realtime_enabled gate)
**Pass/Check:** Pass 4 -- feature flag mid-rollout.
**Finding:** sale_config.realtime_enabled is granted to anon by mig 30 (line 39) and the column is intended as an operator kill-switch, but useTierRealtime never reads it -- the channel is unconditionally subscribed when env vars are present. If the operator flips the flag off (for example to take pressure off the Realtime gateway during a sale spike), every connected browser keeps subscribing and reconnect-storming. Operator believes the kill switch worked when it did not.
**Fix:** Read sale_config.realtime_enabled once via useSaleStatus already-running poll (the API exposes stage; extend it to expose realtimeEnabled) and gate the channel.subscribe() call on the flag. Lazy-disconnect the existing channel when the flag flips to false.

### Ship-readiness extension verification (state-relying-on-async-server-event)

- Webhook fallback for local testers: PASS. scripts/dev-indexer.mjs polls NodePurchased events on both chains, posts to /api/dev/indexer-ingest with HMAC, and ALSO drains failed_events via /api/dev/replay-failed-events. Equivalent to webhook + cron path. Documented in script header.
- Cron fallback: PASS. Reservation expiry is computed client-side from expires_at (sale page L516-524). User does NOT depend on the cron expire_old_reservations to clear stale reservations from their UI. The cron only matters for inventory release server-side (which pnpm dev testers can replay via /api/dev/replay-failed-events).
- Browser supabase queries against revoked tables: PASS. Only one consumer of getSupabaseBrowser (useTierRealtime) and it subscribes only to sale_tiers + sale_config, both granted in mig 30 with the columns the hook reads (tier, total_sold, total_supply, is_active).
- Realtime column scope: PASS. Mig 30 grants are a strict superset of the columns referenced in useTierRealtime payload.new handlers.
- Wallet-switch React-Query teardown: PASS. useAuth L191-228 invalidates wallet-scoped keys (dashboard, nodes, referrals, sale.status) and removeQueries on admin keys. onDisconnect does the harder queryClient.clear().
- Realtime reconnect reconciliation: PASS. useTierRealtime L99-110 invalidates sale + dashboard on SUBSCRIBED re-entry from any non-SUBSCRIBED prior state. First subscribe is a correct no-op.
- wagmi v3 disconnect cleanup / orphan listeners: PASS. useAccountEffect.onDisconnect is the canonical wagmi v3 path; cleanups via channelRef.current.unsubscribe in useTierRealtime. No orphan listeners observed.
- Pending-tx strict address match: PASS. app/(app)/sale/page.tsx:230-234 requires parsed.address equal address.toLowerCase (R14 fix held).
- i18n parity for new copy: PASS. All 7 voucher/realtime keys present in all 6 languages.

### Result

**Checks evaluated:** ~40 across Client (Pass 4 -- State Management section, C-3, C-8 through C-20).
**Findings:** 0 blocking, 1 required, 3 advisory.
**Recommendation:** approve with comments. The required fix is a 3-line change (raw fetch -> authFetch in app/(app)/sale/page.tsx). The three advisories are quality-of-life and operator-control items, none gate ship.

**One thing the developer missed:** the required finding above. The auth-expiry recovery wiring works correctly for hooks (useAuth, useNodes, useReferrals etc.) because they all go through authFetch, but the sale page deliberately uses raw fetch for the three voucher endpoints. The merged effect at useAuth.ts:145 was specifically added in ship-readiness R5 to handle stale-cookie 401s -- and those fixes hold for everything except the sale page itself.

## 2026-04-27 | ship-readiness (post-mig-30) | 1 blocking, 6 required (deduped), 6 advisory | not ready
## 2026-04-30 | ship-readiness | 8 blocking, 15 required, 11 advisory | not ready (3 of those fixed in same review: tier-sold-out short-circuit on own reservation, 002 invariant drift, testing-guide doc drift)
## 2026-04-30 | R8 ship-readiness sweep landed | all blocking + required addressed, fix-discipline checks D-P9.bis / O-P9 / S-P10 added to REVIEW_ADDENDUM | re-run /review-ship pending
## 2026-04-30 | ship-readiness re-review | 2 blocking, 8 required, 13 advisory | not ready (ALL fixed in same review: mig 035 SQL bug, TESTING_GUIDE mig list, CSP :8545 + WSS narrowing, WalletConnect + Coinbase Wallet connectors registered, /api/referrals/summary rate limit + runtime shape validation, useAuth onExpired + onDisconnect races, increment_tier_sold orphans dropped, mig 037 indexes + orphan-purge, voucher.ts orphans removed, OperonNode access tests added, 036 probe operator-readability, ?ref= toUpperCase, tierReservedHintOpen reset, NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID + NEXT_PUBLIC_QUICKNODE_URL + DEPLOYER_PRIVATE_KEY runbook entries)
